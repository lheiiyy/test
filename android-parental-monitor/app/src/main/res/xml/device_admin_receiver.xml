<?xml version="1.0" encoding="utf-8"?>
<device-admin xmlns:android="http://schemas.android.com/apk/res/android">
    <!--
      Deliberately minimal: we do not request wipe-data or disable-camera. This app's job is
      Device Owner status for restriction enforcement (see DeviceOwnerManager.kt), not remote
      wipe or hardware lockout.
    -->
    <uses-policies>
        <watch-login />
    </uses-policies>
</device-admin>
