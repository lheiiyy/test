{
  "name": "droidbridge",
  "private": true,
  "version": "0.1.0",
  "description": "DroidBridge — legitimate, authorized-device Android management desktop app.",
  "workspaces": [
    "shared",
    "desktop"
  ],
  "scripts": {
    "install:all": "npm install",
    "dev": "npm run dev --workspace desktop",
    "build": "npm run build --workspace shared && npm run build --workspace desktop",
    "test": "npm run test --workspace shared && npm run test --workspace desktop && npm run test:integration",
    "test:integration": "vitest run -c vitest.integration.config.ts",
    "typecheck": "npm run typecheck --workspace shared && npm run typecheck --workspace desktop",
    "lint": "eslint . --ext .ts,.tsx",
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "devDependencies": {
    "@typescript-eslint/eslint-plugin": "^7.18.0",
    "@typescript-eslint/parser": "^7.18.0",
    "eslint": "^8.57.1",
    "eslint-config-prettier": "^9.1.0",
    "eslint-plugin-react-hooks": "^4.6.2",
    "prettier": "^3.3.3",
    "typescript": "^5.6.3",
    "vitest": "^2.1.4"
  },
  "engines": {
    "node": ">=18.18.0"
  }
}
